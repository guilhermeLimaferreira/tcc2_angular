import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.angular.tcc2',
  appName: 'tcc2_angular',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
